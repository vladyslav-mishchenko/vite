services:
  nodejs:
    stdin_open: true
    tty: true
    build:
      context: ../
      dockerfile: docker/Dockerfile
    image: vite-frontend:latest
    container_name: vite-frontend
    volumes:
      - ../:/app
    ports:
      - '7777:7777'
